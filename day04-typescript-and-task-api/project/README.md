# Day 4 Project — Task API

The fill-in-the-blank Task API lives in **`../task-api/`** (already scaffolded).

## Setup
```bash
cd ../task-api
npm install
npx ts-node src/index.ts
```

## Your job
1. Open `src/types.ts`, `src/utils.ts`, `src/taskManager.ts`, `src/index.ts`
2. Fill every `TODO` with YOUR code first
3. Only then read the `ANSWER` comments below each TODO
4. Run until `npx ts-node src/index.ts` passes with no errors

## Checklist
- [ ] All types defined in `types.ts`
- [ ] `taskManager.ts` CRUD functions complete
- [ ] `index.ts` demo runs all features
- [ ] `interview-questions.md` — answer 5 questions out loud tonight
